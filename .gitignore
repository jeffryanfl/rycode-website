# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Logs / scratch
*.log
npm-debug.log*

# Local-only artifacts (kept out of the deploy repo)
og-image.html
*.pdf.bak

# Scratch / preview workspace — anything dropped in sandbox/ is
# a work-in-progress file and should never reach the live site.
sandbox/

# Claude Code local session state
.claude/
